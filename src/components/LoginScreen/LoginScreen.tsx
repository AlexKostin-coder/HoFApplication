import React, { FC } from 'react';
import {
  Text,
  View
} from 'react-native';

const LoginScreen: FC = () => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  )
}

export default LoginScreen;
