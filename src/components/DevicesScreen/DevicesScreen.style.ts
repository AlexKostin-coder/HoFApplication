import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  content: {
    flex: 1,
    padding: 10,
    alignItems: 'center'
  },
  device_list: {
    width: '100%',
  }
});
